# shellcheck shell=bash
# Compatibility loader; release implementation is owned by sni_stack_install.sh.

# shellcheck source=/dev/null
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/sni_stack_install.sh"
