# shellcheck shell=bash
# Compatibility loader; release implementation is owned by kernel_tuning.sh.

# shellcheck source=/dev/null
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/kernel_tuning.sh"
