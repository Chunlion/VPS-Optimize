# shellcheck shell=bash
# Compatibility loader; release implementation is owned by system_core.sh.

# shellcheck source=/dev/null
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/system_core.sh"
