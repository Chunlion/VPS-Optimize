# shellcheck shell=bash
# Compatibility loader; release implementation is owned by caddy_maintenance.sh.

# shellcheck source=/dev/null
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/caddy_maintenance.sh"
